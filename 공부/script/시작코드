using UnityEngine;
-유니티에서 제공하는 레퍼런스들을 스크립트 파일 내에서 쓸수 있게 함

public class player : MonoBehaviour
-클래스 명은 파일 이름과 동일하게 설정
-MonoBehavious : 유니티에서 제공하는 함수, 변수 등을 클래스 내에서 사용 가능하게 해줌

void Start()
{
    
}
스크립트가 실행도고, update가 실행되기 전에 단 한번 실행 됨

void Update()
{
    
}
매 프레임마다 호출되는 메세지
